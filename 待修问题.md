# 待修问题清单

> 2026-06-24 会话记录

---

## 已修复

### 1. 🔴 `CASClient._RECON_FRESH_INTERVAL` 类属性不存在
- **文件**: `xet/network/cas_client.py`
- **现象**: `AttributeError: type object 'CASClient' has no attribute '_RECON_FRESH_INTERVAL'` → 下载失败
- **原因**: `_RECON_FRESH_INTERVAL = 30` 定义在 `__init__` 方法内（局部变量），但以 `CASClient._RECON_FRESH_INTERVAL`（类属性）访问
- **修复**: 提升为类级别常量，同时重命名为 `_recon_refresh_interval`

### 2. 🔴 `_print()` flush 参数 TypeError
- **文件**: `xet/network/host_optimizer.py#L747`
- **现象**: `TypeError: _print() got an unexpected keyword argument 'flush'`
- **修复**: 函数签名改为 `def _print(msg: str, **kwargs)`

### 3. 🔴 403 重试冲突（URL 刷新逻辑永不触发）
- **文件**: `xet/network/cas_client.py#L341`
- **现象**: 内层 `@with_retry` 吞掉 403，外层 URL 刷新分支走不到
- **修复**: 移除 `get_xorb_data()` 上的 `@with_retry`，由外层统一管理

### 4. 🔴 `optimize.py` 缩进错误（UnboundLocalError）
- **文件**: `xet/cli/commands/optimize.py`
- **修复**: 修正为正确的函数级/块级缩进

### 5. 🔴 `optimize.py` `_hf_endpoint` 为 None
- **文件**: `xet/cli/commands/optimize.py`
- **修复**: 添加回退链至默认值 `https://huggingface.co`

### 6. 🔴 `optimize.py` `match_files` 参数类型错误
- **文件**: `xet/cli/commands/optimize.py`
- **修复**: `["*"]` → `"*"`

### 7. 🟠 `chunk_assembler.py` writer 变量未初始化
- **文件**: `xet/pipeline/chunk_assembler.py`
- **修复**: try 前添加 `writer = None`

### 8. 🟠 DoH 查询无结果日志噪音
- **文件**: `xet/network/host_optimizer.py`
- **修复**: 降级为 DEBUG 级别

### 9. 🟠 403 连续失败时无进度/存盘信息
- **文件**: `xet/cli/commands/download.py`
- **修复**: 增强 except 处理，显示已下载数据和断点保存提示

---

## 当前会话新发现问题

### 10. 🟡 `auth.py` `REPO_TYPE_PLURAL` 字典重复定义
- **文件**: `xet/network/auth.py`
- **现象**: 同一字典 `{"model": "models", ...}` 在 `_resolve_revision()` 和 `_request_token()` 中各定义一次
- **修复**: 提取为模块级常量 `REPO_TYPE_PLURAL_MAP`

### 11. 🟡 `host_optimizer.py` 宏风格局部变量
- **文件**: `xet/network/host_optimizer.py`
- **现象**: 方法内 `TEST_DURATION`、`CHUNK_SIZE`、`RANGE_MAX`、`_CERT_TOP_N` 使用 ALL_CAPS 命名（类宏风格）
- **修复**: 统一重命名为蛇形命名

### 12. 🟡 `cas_client.py` `_RECON_FRESH_INTERVAL` 命名
- **文件**: `xet/network/cas_client.py`
- **说明**: "fresh" 英文不当（应为 "refresh"），且 ALL_CAPS 类宏风格
- **修复**: 重命名为 `_recon_refresh_interval`

---

## 遗留改进建议（非 bug，低优先级）

### A. checkpoint_manager.py 持锁做 I/O
- **位置**: `xet/pipeline/checkpoint_manager.py#L201`
- **说明**: `_save_unsafe()` 在 `with self._lock:` 内写文件，单线程安全但不利于未来扩展

### B. CAS Transfer 测速全部显示 1B/s
- **位置**: `xet/network/host_optimizer.py` Transfer 测速逻辑
- **说明**: 无有效 CAS token 时拿到 403 响应体（~1B），应显示 N/A

### C. `Authorization: None` HTTP 头语义
- **位置**: `xet/network/cas_client.py`
- **说明**: 功能正确但代码不直观，可加注释或省略该 key
